{% extends 'base.html.twig' %}

{% block title %}Convertir une URL en PDF{% endblock %}

{% block body %}
    <style>
        .container { margin: 2em auto; max-width: 800px; font: 18px/1.5 sans-serif; }
        .alert { padding: 10px; margin-bottom: 10px; border-radius: 5px; }
        .alert-success { background-color: #d4edda; color: #155724; border: 1px solid #c3e6cb; }
        .alert-error { background-color: #f8d7da; color: #721c24; border: 1px solid #f5c6cb; }
    </style>

    <div class="container">
        <h1>Convertir une URL en PDF ✅</h1>

        {% if pdf_url is defined %}
            <div class="alert alert-success">
                🎉 PDF généré avec succès ! <a href="{{ pdf_url }}" target="_blank">Télécharger le PDF</a>
            </div>
        {% elseif error is defined %}
            <div class="alert alert-error">
                ❌ Erreur : {{ error }}
            </div>
        {% endif %}

        <form action="{{ path('generate-pdf') }}" method="POST">
            <label for="url">URL à convertir :</label>
            <input type="text" name="url" id="url" value="https://www.example.com" required>
            <button type="submit">Convertir en PDF</button>
        </form>
    </div>
{% endblock %}